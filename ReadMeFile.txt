Syntax
    node --version
    npm --version

For the Project
    After the package and tsconfig file is saved
        npm install
        npm start
        npm install typescript -g (Note:If tsc is not working)